// // here I am creating a book components's dto which has id title and author of the book
// and it is default interface
export default interface BookDTO {
    id: number;
    title: string;
    author: string;
}